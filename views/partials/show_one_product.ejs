<!-- breadcrumb -->
<nav aria-label="breadcrumb">
  <ol class="breadcrumb bg-light">
    <li class="breadcrumb-item"><a href="/" class="text-dark">Home</a></li>
    <li class="breadcrumb-item active" aria-current="page">Product</li>
  </ol>
</nav>

<!-- Product Info -->
<div class="row mr-0">
  <div class="col-12 col-lg-6 col-sm-12 mb-5 rounded">
    <div class="bg-light rounded px-3 py-3 h-100">
      <img class="w-100 h-100" src="/images/<%= product.image %>" alt="<%= product.productName %> Picture">
    </div>
  </div>
  <div class="col-12 col-lg-6 col-sm-12 mb-5 rounded bg-light">
    <div class="p-5">
      <!-- product name -->
      <h1><%= product.productName %></h1>
      <!-- product description -->
      <p><%= product.description %></p>
      <% product.categories.forEach(function(category){ %>
      <!--product category -->
      <a href="#" class="badge badge-secondary float-right mr-1 mb-1"><%= category %></a>
      <% }); %>
      <br>
      <div class="row">
        <div class="col-6">
          <!-- shopcart form -->
          <form>
            <!-- select number -->

            <select class="custom-select w-auto my-4 mr-1 pt-2" aria-hidden="true">
              <% for (var i = 1; i <= product.num; i++) {%>
              <option class="dropdown-item bg-light" value="<%= i %>"> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <%= i %> &nbsp;&nbsp;&nbsp;</option>
              <% } %>
            </select>

        </div>
        <div class="col-6">
          <!-- product price -->
          <h4 class="mt-3 mb-1 float-right mr-1 pt-2">$<%= product.productPrice %></h4>
        </div>
      </div>
      <div class="row">
        <div class="col-6">
          <!-- submit button -->
          <button href="#" class="btn btn-warning float-left mb-5 px-4"><i
              class="fas fa-shopping-cart mr-2"></i>Add</button>
          </form>
        </div>
        <div class="col-6">
          <!-- favorite icon -->
          <h3 class="float-right"><i class="far fa-heart mr-1 pr-1 text-danger"></i></h3>
        </div>
      </div>
    </div>
  </div>
</div>
</div>